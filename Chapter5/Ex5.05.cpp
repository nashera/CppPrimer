#include <iostream>
#include <vector>
#include <string>
using namespace std;
int main(){
        //逻辑与操作符, 如果与操作符前面的语句为假, 则在计算第一个完就得到操作符, 如果第一个为真, 则在计算完第二个得出.
        //逻辑或操作符, 则需要两个都语句都计算完, 才能得到操作符.
        // 等于操作符, 需要先计算等于两边的数值, 才能得到操作符.
    return 0;
               
}
