#include <iostream>
#include <vector>
#include <string>
using namespace std;
int main(){
        // 如果输出vector内容的while循环使用前自增操作符, 那么在当迭代器指向end时, 循环并不结束, 因为指向了vector最后一个元素, 而对此时的迭代器解引用出错.
    return 0;
}
