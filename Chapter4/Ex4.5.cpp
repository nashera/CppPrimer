#include <iostream>
#include <string>
using namespace std;
int main(){
        /* Arrays are fixed size,
         * because there is no push_back or other operation to add elements to the array.
         * Once we define an array, we cannot add element to it.
         * If we must add elements to the array, then we must mangage the mememory ourseleves.
         * we have to ask the system for new storage to hold the larger array  and copy the existing elements into that new storage.
         * there is no array copy or assignments.*/
    return 0;
        
}
